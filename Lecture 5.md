# How internet works 
When one computer connected to the other computer using wires then they form a network. But this network limited to only these two computer. So the concept of internet is that the big computer is known as serves are made across the world like google have its own server and YouTube have its own server . Server is a big computer which stores large amount of data . Like when you click any video on YouTube then the videos is sent to your device in packets so you can see . So these big computer who is known as a server is connected to each other using cables like optical fiber . And these cables are placed almost all over the world . And through these cables the ISP(Internet service providers ) provides internet through cables in this router is also plays an important role in the data transmitting from one computer to server, server to computers. So when we sent an request from client side then the our request move to our local router on our local router data or request sent as in the form of packets. And these packets have users private if and mac address and destination ip address this is just between user and there local wifi router. But when data packets reached to the router then router replce private ip with public ip and replace user mac address with own mac address and the destination address remain same and the mac address  is changes from hop to hop . Our local router sent our request to ISP and ISP sent to the requested server and from the server side server respond and then data is comes in the form of packets to the users device. Internet is network of millions and billions of devices. But when we connected to our local wifi router we are not directly connected to the internet but we are indirectly connected to the internet. Thus internet works in this way. 
# What is web Server 
The web server is a big computer that are built to store a websites. These web server providing there services to the user so they can provide their services digitally. These server keeps the files of a web site and live the website 24 / 7 . So any one can access any time they use there own space. These web server provide hosting many server providing like Hostinger Vercel etc.   
# What is Web page
A web page is a document that hold some visual and info text buttons colors cards that display on client side and these pages have some interactivity. The technologies like HTML,CSS and JavaScript are use to create them.
# What is website
Website is a collection of web pages that stores on a server so we can provide digital services to the user. These files are stored in a single folder. A web can consist of multiple pages and database is used to store the data and to retrieved the data on request. These pages can be 1 10 an many consist of what type of web. Government and particular organization and almost every individual can made and provide there services digitally.  
# Search engine
Search engine are a software that allow us to search any web any image videos on browser like Google chrome, edge,Firefox etc. Search engine are also many like google,Yahoo DuckDuckGo,etc. So Almost every using google search engine. So when we search any thing on google like “best nike shoes” then google sent its bots they crawl every web page that have this keyword the google bots visit every webpages and crawl them and check them are these pages fulfilling user requirements. Its like they test every single page so user experience wont disturbed . After crawling they store the webpages and index them and show on top tjat website that is fully ok . So that’s the way search engine works.
# DNS
Domain name server is like a phone book. In this book every websites ip address are stores. Why DNS requires. When we search any web then browser is not able to understand names they only understand number. Human are good in remembering names. And machine are good in remembering numbers . So when we search any web browser request to the DNS and say what the ip of this web name. DNS  provide ip address and then browser retrieved that website. DNS have all over the world web IP.
# Web browser
Web browser is software that help to Retrieved the web data like websites pages images  That shows to the user display . So user can see and interact and can access websites.

